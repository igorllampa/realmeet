# ====================================
# = Spring
# ====================================
spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql://localhost/realmeet
    username: root
    password: root
  main:
    allow-bean-definition-overriding: true
  flyway:
    locations: classpath:/db/migration/postgresql